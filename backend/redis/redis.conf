# SyferStack Redis configuration

# Listen on all interfaces (restricted by Docker networking)
bind 0.0.0.0
port 6379
protected-mode no

# Persistence
appendonly yes

# Basic memory policy (adjust to your needs)
maxmemory-policy allkeys-lru
