# Docker requirements file for audit system
# Core dependencies
asyncio-mqtt==0.13.0
aiohttp>=3.9.0
aiofiles>=23.2.1
rich>=13.0.0
pydantic>=2.0.0
pyyaml>=6.0.1
requests>=2.31.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Monitoring dependencies (optional)
prometheus-client>=0.17.0
psutil>=5.9.0

# Database
# sqlite3 is built into Python, no need to install

# Development tools (included in container)
ruff>=0.1.0
bandit>=1.7.0
mypy>=1.5.0
safety>=2.3.0
pylint>=2.17.0
black>=23.0.0
isort>=5.12.0